# LoadBalancer に払い出す IP の範囲。VLAN 120 に置く。
# ノードと同じ VLAN に置くと BGP へ移した時点で VLAN 20 の機器から届かなくなる。
# 根拠は docs/knowledge/service-exposure.md にある。
---
apiVersion: cilium.io/v2
kind: CiliumLoadBalancerIPPool
metadata:
  name: default
spec:
  blocks:
    - start: 192.168.120.100
      stop: 192.168.120.250
